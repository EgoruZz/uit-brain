<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Таблица дедлайнов</title>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        body {
            background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
            color: #333;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 20px;
        }
        
        .container {
            width: 100%;
            max-width: 900px;
            background: white;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            overflow: hidden;
            margin: 20px 0;
        }
        
        header {
            background: #2c3e50;
            color: white;
            padding: 25px;
            text-align: center;
        }
        
        h1 {
            font-size: 2.2rem;
            margin-bottom: 10px;
        }
        
        .subtitle {
            font-size: 1.1rem;
            opacity: 0.8;
        }
        
        .table-container {
            overflow-x: auto;
            padding: 20px;
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
            margin: 0 auto;
            font-size: 1rem;
        }
        
        th, td {
            padding: 16px 20px;
            text-align: left;
            border-bottom: 1px solid #e0e0e0;
        }
        
        th {
            background-color: #f5f7fa;
            font-weight: 600;
            color: #2c3e50;
            position: sticky;
            top: 0;
        }
        
        tr:last-child td {
            border-bottom: none;
        }
        
        tr:hover {
            background-color: #f9f9f9;
        }
        
        .subject {
            font-weight: 500;
            color: #2c3e50;
        }
        
        .countdown {
            font-family: 'Courier New', monospace;
            font-weight: 600;
            padding: 8px 12px;
            border-radius: 20px;
            display: inline-block;
            min-width: 120px;
            text-align: center;
        }
        
        .normal {
            background-color: #e8f5e9;
            color: #2e7d32;
        }
        
        .warning {
            background-color: #ffecb3;
            color: #f57c00;
        }
        
        .urgent {
            background-color: #ffcdd2;
            color: #c62828;
            animation: pulse 1.5s infinite;
        }
        
        @keyframes pulse {
            0% { opacity: 1; }
            50% { opacity: 0.7; }
            100% { opacity: 1; }
        }
        
        .completed {
            background-color: #4caf50;
            color: white;
        }
        
        .info {
            background: #e3f2fd;
            padding: 15px;
            border-radius: 10px;
            margin: 20px;
            font-size: 0.9rem;
        }
        
        .last-update {
            text-align: center;
            padding: 15px;
            color: #777;
            font-size: 0.9rem;
        }
        
        @media (max-width: 768px) {
            th, td {
                padding: 12px 15px;
            }
            
            h1 {
                font-size: 1.8rem;
            }
            
            .container {
                border-radius: 10px;
            }
        }
        
        @media (max-width: 480px) {
            th, td {
                padding: 10px 12px;
                font-size: 0.9rem;
            }
            
            .countdown {
                min-width: 100px;
                padding: 6px 10px;
                font-size: 0.85rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>Дедлайны проектов</h1>
            <div class="subtitle">Осталось времени до сдачи работ</div>
        </header>
        
        <div class="table-container">
            <table>
                <thead>
                    <tr>
                        <th>Название предмета</th>
                        <th>Осталось до конца</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td class="subject">Веб-программирование</td>
                        <td><span class="countdown urgent" id="countdown-1">4 д. 12:35:10</span></td>
                    </tr>
                    <tr>
                        <td class="subject">Базы данных</td>
                        <td><span class="countdown warning" id="countdown-2">12 д. 08:20:45</span></td>
                    </tr>
                    <tr>
                        <td class="subject">Машинное обучение</td>
                        <td><span class="countdown normal" id="countdown-3">21 д. 04:15:30</span></td>
                    </tr>
                    <tr>
                        <td class="subject">Тестирование ПО</td>
                        <td><span class="countdown normal" id="countdown-4">30 д. 00:10:15</span></td>
                    </tr>
                    <tr>
                        <td class="subject">Архитектура ЭВМ</td>
                        <td><span class="countdown completed">Завершён</span></td>
                    </tr>
                </tbody>
            </table>
        </div>
        
        <div class="info">
            💡 Цвета указывают на срочность: <span style="color:#c62828;">красный</span> — срочно, <span style="color:#f57c00;">оранжевый</span> — скоро дедлайн, <span style="color:#2e7d32;">зелёный</span> — ещё есть время
        </div>
        
        <div class="last-update">
            Последнее обновление: <span id="update-time">загрузка...</span>
        </div>
    </div>

    <script>
        // Данные о дедлайнах (можно заменить на свои)
        const deadlines = [
            { id: 'countdown-1', date: '2023-12-15T23:59:00', subject: 'Веб-программирование' },
            { id: 'countdown-2', date: '2023-12-23T23:59:00', subject: 'Базы данных' },
            { id: 'countdown-3', date: '2024-01-01T23:59:00', subject: 'Машинное обучение' },
            { id: 'countdown-4', date: '2024-01-10T23:59:00', subject: 'Тестирование ПО' }
        ];
        
        // Функция для обновления отсчётов времени
        function updateCountdowns() {
            const now = new Date();
            
            deadlines.forEach(deadline => {
                const targetDate = new Date(deadline.date);
                const timeDiff = targetDate - now;
                
                if (timeDiff <= 0) {
                    document.getElementById(deadline.id).textContent = "Завершён";
                    document.getElementById(deadline.id).className = "countdown completed";
                    return;
                }
                
                const days = Math.floor(timeDiff / (1000 * 60 * 60 * 24));
                const hours = Math.floor((timeDiff % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
                const minutes = Math.floor((timeDiff % (1000 * 60 * 60)) / (1000 * 60));
                const seconds = Math.floor((timeDiff % (1000 * 60)) / 1000);
                
                const countdownText = `${days} д. ${hours.toString().padStart(2, '0')}:${minutes.toString().padStart(2, '0')}:${seconds.toString().padStart(2, '0')}`;
                
                const countdownElement = document.getElementById(deadline.id);
                countdownElement.textContent = countdownText;
                
                // Обновляем классы в зависимости от оставшегося времени
                if (days < 2) {
                    countdownElement.className = "countdown urgent";
                } else if (days < 7) {
                    countdownElement.className = "countdown warning";
                } else {
                    countdownElement.className = "countdown normal";
                }
            });
            
            // Обновляем время последнего обновления
            document.getElementById('update-time').textContent = new Date().toLocaleString('ru-RU');
        }
        
        // Обновляем отсчёты сразу после загрузки страницы
        updateCountdowns();
        
        // Обновляем отсчёты каждую секунду
        setInterval(updateCountdowns, 1000);
    </script>
</body>
</html>
